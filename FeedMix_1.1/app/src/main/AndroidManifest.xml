<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.Uga"
    android:versionCode="4"
    android:versionName="1.1" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="24" />

    <application
        android:allowBackup="true"

        android:icon="@drawable/uga_icon"
        android:label="@string/app_name"
       android:theme="@style/Theme.NoTitle" >
        <activity
            android:name="com.android.Uga.Firstactivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:label="@string/app_name"
          />
        <activity android:name=".Choose_diet_activity"
            android:windowSoftInputMode="adjustPan"

        android:screenOrientation="portrait"
            android:label="@string/app_name"
            />
        <activity android:name=".MPpiechart"
            android:screenOrientation="portrait"
            android:label="@string/app_name"
            />
        <activity android:name=".Mppiechart2"
            android:screenOrientation="portrait"
            android:label="@string/app_name"
            />
       <!-- <activity android:name=".Create_diet_other"
            android:screenOrientation="portrait"
            android:label="@string/app_name"
            />-->

    </application>

</manifest>
